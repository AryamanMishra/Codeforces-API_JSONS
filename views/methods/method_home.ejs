<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= methodName %> </title>
</head>
<body>
    <p>
        Note: For methods depending on contest id, fill the id for either one of them and data for that id will be loaded for others too.
        For example, contest.hacks depends on contest id so by clicking on it you will be asked to give a contest id. By entering a valid contest id, data for other id dependent methods
        like, contest.rating changes, contest.status,contest.standings will also be loaded for the id given by you.
    </p>
    <br>
    <% if (methodName === 'blogEntry') { %>
        <button onclick="dropDownCom()">blogEntry.comments &#8628;</button>
        <br>
        <br>
        <section id="hidden-data-comments" style="display: none;">
            <textarea name="" id="" cols="150" rows="30">
                <%= JSON.stringify(linkData.linkblogEntry_comments.data) %> 
            </textarea>
        </section>
        <button onclick="dropDownView()">blogEntry.view &#8628;</button>
        <br>
        <br>
        <section id="hidden-data-view" style="display: none;">
            <textarea name="" id="" cols="50" rows="20">
                <%= JSON.stringify(linkData.linkblogEntry_view.data) %> 
            </textarea>
        </section>
    <% } else  if (methodName === 'contest'){ %>
        <% if (check === 0) { %> 
            <button><a href="/methods/contest/forms/idFiller"><%= methodName %>.hacks</a></button>
            <br>
            <br>
        <% } else { %> 
            <h2>Entered id: <%= id %> </h2>
            <button onclick="dropDownhacks()"><%= methodName %>.hacks &#8628;</button>
            <br>
            <br>
            <section id="hidden-data-hacks" style="display: none;">
                    <textarea name="" id="" cols="150" rows="30">
                        <%= JSON.stringify(linkData.linkcontest_ratingChanges.data) %> 
                    </textarea>
            </section>
        <% } %> 
        <button onclick="dropDownListT()">contest.list(gym=true) &#8628;</button>
        <br>
        <br>
        <section id="hidden-data-listT" style="display: none;">
            <textarea name="" id="" cols="150" rows="30">
                <%= JSON.stringify(linkData.linkcontest_listT.data) %> 
            </textarea>
        </section>
        <button onclick="dropDownListF()">contest.list(gym=false) &#8628;</button>
        <br>
        <br>
        <section id="hidden-data-listF" style="display: none;">
            <textarea name="" id="" cols="150" rows="30">
                <%= JSON.stringify(linkData.linkcontest_listF.data) %> 
            </textarea>
        </section>
        <% if (check === 0) { %> 
            <button><a href="/methods/contest/forms/idFiller">contest.ratingChanges</a></button>
            <br>
            <br>
        <% }else { %> 
            <button onclick="dropDownrC()">contest.ratingChanges &#8628;</button>
            <br>
            <br>
            <section id="hidden-data-rC" style="display: none;">
                    <textarea name="" id="" cols="150" rows="30">
                        <%= JSON.stringify(linkData.linkcontest_ratingChanges.data) %> 
                    </textarea>
            </section>
        <% } %> 
        <% if (check === 0) { %> 
            <button><a href="/methods/contest/forms/idFiller">contest.standings(Unofficial)</a></button>
            <br>
            <br>
        <% }else {%> 
            <button onclick="dropDownstan()">contest.standings(Unofficial) &#8628;</button>
            <br>
            <br>
            <section id="hidden-data-stan" style="display: none;">
                    <textarea name="" id="" cols="150" rows="30">
                        <%= JSON.stringify(linkData.linkcontest_standings.data) %> 
                    </textarea> 
            </section>
        <% } %>
        <% if (check === 0) { %> 
            <button><a href="/methods/contest/forms/idFiller">contest.status</a></button>
            <br>
            <br>
        <% } else { %> 
            <button onclick="dropDownstatus()">contest.status &#8628;</button>
            <section id="hidden-data-status" style="display: none;">
                    <textarea name="" id="" cols="150" rows="30">
                        <%= JSON.stringify(linkData.linkcontest_status.data) %> 
                    </textarea>
            </section>
        <% } %> 
    <% } else if (methodName === 'problemset') {%>
        <button><a href="/methods/contest/forms/idFiller"><%= methodName %>.problems</a></button>
        <button><a href="/methods/contest/forms/idFiller"><%= methodName %>.recentStatus</a></button>  
    <% } %> 
    <script>
        function dropDownCom() {
            let data = document.getElementById('hidden-data-comments');
            if (data.style.display == 'none') {
                data.style.display = 'block';
            } else {
                data.style.display = 'none';
            }
        }
        function dropDownView() {
            let data = document.getElementById('hidden-data-view');
            if (data.style.display == 'none') {
                data.style.display = 'block';
            } else {
                data.style.display = 'none';
            }
        }
        function dropDownhacks() {
            let data = document.getElementById('hidden-data-hacks');
            if (data.style.display == 'none') {
                data.style.display = 'block';
            } else {
                data.style.display = 'none';
            }
        }
        function dropDownListT() {
            let data = document.getElementById('hidden-data-listT');
            if (data.style.display == 'none') {
                data.style.display = 'block';
            } else {
                data.style.display = 'none';
            }
        }
        function dropDownListF() {
            let data = document.getElementById('hidden-data-listF');
            if (data.style.display == 'none') {
                data.style.display = 'block';
            } else {
                data.style.display = 'none';
            }
        }
        function dropDownrC() {
            let data = document.getElementById('hidden-data-rC');
            if (data.style.display == 'none') {
                data.style.display = 'block';
            } else {
                data.style.display = 'none';
            }
        }
        function dropDownstan() {
            let data = document.getElementById('hidden-data-stan');
            if (data.style.display == 'none') {
                data.style.display = 'block';
            } else {
                data.style.display = 'none';
            }
        }
        function dropDownstatus() {
            let data = document.getElementById('hidden-data-status');
            if (data.style.display == 'none') {
                data.style.display = 'block';
            } else {
                data.style.display = 'none';
            }
        }
    </script> 
</body>
</html>